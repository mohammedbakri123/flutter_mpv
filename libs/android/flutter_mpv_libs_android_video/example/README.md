## Installation

Add in your `pubspec.yaml`:

```yaml
dependencies:
  flutter_mpv_libs_android_video: ^1.3.8
```

This will automatically bundle required shared libraries for video (& audio) playback on Android.
