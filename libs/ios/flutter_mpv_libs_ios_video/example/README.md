## Installation

Add in your `pubspec.yaml`:

```yaml
dependencies:
  flutter_mpv_libs_ios_video: ^1.1.4
```

This will automatically bundle required shared libraries for video (& audio) playback on iOS.
