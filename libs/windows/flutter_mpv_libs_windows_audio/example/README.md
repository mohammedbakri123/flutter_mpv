## Installation

Add in your `pubspec.yaml`:

```yaml
dependencies:
  flutter_mpv_libs_windows_audio: ^1.0.9
```

This will automatically bundle required shared libraries for audio (only) playback on Windows.
