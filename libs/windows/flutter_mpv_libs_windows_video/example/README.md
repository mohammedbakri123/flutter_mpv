## Installation

Add in your `pubspec.yaml`:

```yaml
dependencies:
  flutter_mpv_libs_windows_video: ^1.0.12
```

This will automatically bundle required shared libraries for video (& audio) playback on Windows.
